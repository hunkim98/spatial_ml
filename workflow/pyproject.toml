[project]
name = "spatial-ml-workflow"
version = "0.1.0"
description = "Vertex AI pipelines for Municode ordinance collection and processing"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "google-cloud-aiplatform>=1.120.0",
    "google-cloud-storage>=2.10.0",
    "kfp>=2.14.6",
]
