## Quick Start

```bash
docker compose run --rm collector
```

## Rebuild and run the collector

```bash
docker compose build collector && docker builder prune -f && docker compose run --rm collector
```